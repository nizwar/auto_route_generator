name: auto_route_generator
description: AutoRoute is a declartive routing solution, where everything needed for navigation is automatically generated for you.
version: 1.0.0-beta.10
homepage: https://github.com/Milad-Akarie/auto_route_library
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  build: ^1.0.0
  source_gen: ^0.9.0
  analyzer: ">=0.40.0 <2.0.0"
  path: ^1.6.4
  meta: ^1.1.8
  build_runner: ^1.10.1
  code_builder: ^3.6.0
  dart_style: ^1.3.9
  auto_route: 
    git:
      url: https://github.com/nizwar/auto_route_lib.git
#    path: ../auto_route

dev_dependencies:
  source_gen_test:
  test:
